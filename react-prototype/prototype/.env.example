# Copy to .env next to package.json (React prototype root) — never commit .env

GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Optional: set to 1 to skip deterministic rules and always use Gemini when a key exists
# SKIP_CANONICAL_CASES=1
